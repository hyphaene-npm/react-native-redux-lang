import react-native-redux-18n from '../src/';

describe('react-native-redux-18n', () => {
	it('should return the proper value', () => {
		expect(react-native-redux-18n).toBeInstanceOf(Function);
	});
});
